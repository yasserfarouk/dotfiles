-- NOTE: The treesitter playground is deprecated in Neovim 0.12+
-- Use the built-in :InspectTree command instead (or :Inspect for node under cursor)
-- This file is kept for backward compatibility but does nothing on 0.12+

-- For older Neovim versions, this would set up the playground
-- The new nvim-treesitter (main branch) doesn't support the playground module

-- Useful commands in Neovim 0.12+:
-- :InspectTree - Opens a window with the syntax tree
-- :Inspect - Shows info about the node under cursor
-- :EditQuery - Edit treesitter queries
